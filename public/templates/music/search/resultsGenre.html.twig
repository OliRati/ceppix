<style>
    .music-container {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .recording {
        background-color: gray;
        padding: 1rem;
        width: 16rem;
        border-radius: 0.5rem;
    }
</style>
<h3>
    Résultats de la recherche de {{ search }} dans {{ filter }}
</h3>
<div class="music-container">
    {% for artist in artists %}
        <div class="recording">
            <div class="name">
                Nom :
                {{ artist.name }}
            </div>
            <div>
                Genre :
                {{ artist.gender }}
            </div>
            <div>
                Pays : {{ artist.country }}
            </div>
        </div>
    {% endfor %}
</div>
